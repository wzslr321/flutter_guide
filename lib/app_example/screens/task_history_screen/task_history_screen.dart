import 'package:flutter/material.dart';

class TaskHistoryScreen extends StatelessWidget {
  static const routeName = '/tasks-history';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Task history"),
      ),
      body: Container(),
    );
  }
}
